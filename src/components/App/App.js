import React, { Component } from 'react';
import MainContent from 'Components/Content/Content.js';

export default class AppComponent extends Component{
    render(){
        return(
            <div>
                <MainContent />
            </div>
        )
    }
}